# pixi environment
.pixi/

# CI working dirs (results are committed via events/ only)
outputs_ci/
gf_cache/

# python
__pycache__/
*.pyc
.pytest_cache/

# macOS
.DS_Store

# per-event working files: regenerable from solution.json + the GF release
# (the CI watcher must not commit staged Green's functions or SAC data)
events/*/band_*/
events/*/*.dat
events/*/greens/
